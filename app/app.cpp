#include "app.h"

#include <format>

namespace vsite::oop::p1 {

	double f1(int a, int b) {
		return pow(a, b);
	}

	bool f2(bool b) {
		return (!b);

	}

	int f3(int a, int b, int c, int d) {
		return (a + b + c + d);
	}

} 
